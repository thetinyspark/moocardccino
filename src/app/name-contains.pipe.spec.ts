import { NameContainsPipe } from './name-contains.pipe';

describe('NameContainsPipe', () => {
  it('create an instance', () => {
    const pipe = new NameContainsPipe();
    expect(pipe).toBeTruthy();
  });
});

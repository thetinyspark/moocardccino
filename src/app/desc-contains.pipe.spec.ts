import { DescContainsPipe } from './desc-contains.pipe';

describe('DescContainsPipe', () => {
  it('create an instance', () => {
    const pipe = new DescContainsPipe();
    expect(pipe).toBeTruthy();
  });
});
